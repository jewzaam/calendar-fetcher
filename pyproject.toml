# Generated By: Claude Code (Claude Opus 4.6)

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "calendar-fetcher"
version = "0.1.0"
description = "Fetch and download artifacts from Google Calendar meetings"
readme = "README.md"
requires-python = ">=3.12"
license = {file = "LICENSE"}
authors = [
    {name = "Naveen Malik"}
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-mock>=3.0",
    "black>=23.0",
    "flake8>=6.0",
    "mypy>=1.11",
    "mutmut>=2.0,<3.0",
]

[project.scripts]
calendar-fetcher = "calendar_fetcher.__main__:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["calendar_fetcher*"]

[tool.mutmut]
paths_to_mutate = ["calendar_fetcher/"]
also_copy = ["conftest.py"]
